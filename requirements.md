# Project Requirements

## Functional Requirements
- System must allow admins to add students.
- System must display a list of all enrolled students.

## SCM Requirements
- All code must be scanned via **Black Duck** before production merge.
- Version tagging must follow Semantic Versioning (MAJOR.MINOR.PATCH).
- Pull Requests must require at least one peer approval.